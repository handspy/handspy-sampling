apply plugin: "war"

bootWar {
    mainClassName = "pt.up.hs.sampling.SamplingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
